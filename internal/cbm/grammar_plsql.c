// Vendored tree-sitter grammar: plsql (Oracle PL/SQL — AndreasMaierDe/tree-sitter-plsql)
// Each grammar compiled as separate unit (conflicting static symbols).
#include "vendored/grammars/plsql/parser.c"
